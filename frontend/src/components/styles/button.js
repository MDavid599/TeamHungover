import styled from "styled-components";

const Button = styled.button`
  font-weight: 600;
  font-size: 1rem;
  color: white;
  background: linear-gradient(#04e384, #25cdc5);
  padding: 1rem 1.5rem;
  border: none;
  border-radius: 2rem;
  margin: 1rem;
`;

export default Button;
